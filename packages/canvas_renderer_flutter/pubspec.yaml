name: canvas_renderer_flutter
description: Flutter renderer for canvas_core paint operations and scene exports.
version: 0.1.1
repository: https://github.com/Nijify/canvas_renderer_flutter
issue_tracker: https://github.com/Nijify/canvas_renderer_flutter/issues
topics:
  - canvas
  - flutter
  - graphics
  - renderer

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  canvas_core: ">=0.2.0 <0.4.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
